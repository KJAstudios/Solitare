package com.mygdx.game;

public class LevelLoader {
    Deck gameDeck = new Deck();

    LevelLoader(String levelType) {
        switch (levelType) {
            case "Standard": {
            }
        }
    }
}
